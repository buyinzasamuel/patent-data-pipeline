# patent-data-pipeline
This is to handle patent data
# Patent Data Pipeline

This project implements a complete data pipeline that collects, cleans, stores, and analyzes real USPTO patent data.

## Requirements

- Python 3.8+
- Install dependencies: `pip install -r requirements.txt`

## How to Run

1. Clone this repository.
2. Run the pipeline: